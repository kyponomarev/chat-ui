<div class="page {{class}}">
    <aside class="sidebar container__sidebar">
        <header class="sidebar__header header">
            <nav class="nav nav_content-position_space-between sidebar__nav">
                <a routerlink href="{{createLink.url}}" class="nav__link">{{createLink.text}}</a>
                <a routerlink href="{{profileLink.url}}" class="nav__link">{{profileLink.text}}</a>
            </nav>
            <input class="search-bar sidebar__search-bar" placeholder="{{searchBarPlaceholder}}"/>
        </header>
        <div class="sidebar__content">
            {{{chatList}}}
        </div>
    </aside>
    <main class="main main_centered-horizontal container__main">
        {{{chat}}}
    </main>
</div>
